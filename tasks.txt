================================================================================
  MCKO - CURRENT ENGINEERING CHECKLIST
================================================================================

CURRENT BASELINE
----------------

[x] App runs through `main.py`.
[x] Active runtime is `GeometryPhotoSolver`.
[x] Three-model image path is restored:
    Qwen parser -> Kimi solver -> Llama verifier.
[x] Text-only fast path uses Kimi.
[x] Old active `APIClient` runtime is removed.
[x] User-facing output is answer-only.
[x] Solver has top-level fallback to `1) Не удалось определить ответ`.
[x] JSON repair and payload normalization are covered by unit tests.
[x] User regression fixture runner exists.
[x] `AGENTS.md` and `CLAUDE.md` document current architecture.


DO NOT REINTRODUCE
------------------

[ ] Hardcoded answers for user screenshots.
[ ] Broad exact-solver hacks for specific task classes.
[ ] Active `OPENROUTER_MODEL` / `OPENROUTER_MODELS`.
[ ] Active `SYSTEM_PROMPT_1` / `SYSTEM_PROMPT_2`.
[ ] Single-model runtime unless explicitly requested again.
[ ] Reasoning/explanations in final UI or clipboard output.


CORE RUNTIME TODO
-----------------

[ ] Re-run full live suite on target machine:
    `python3 tests/run_solver_fixture_suite.py user-regression`

[ ] Investigate any remaining false answers in live logs:
    - compare Qwen parse
    - compare Kimi answer
    - compare Llama verifier
    - check repair source
    - check final selector branch

[x] Improve provider resilience:
    - 429 now gets 4s/8s retry delay (was 1s/2s)
    - timeout increased to 120s for slow models

[x] Improve option-selection task stability:
    - added Russian markers: укажите номера, запишите цифры,
      установите соответствие, выберите верные, какие из, etc.
    - _looks_like_final_answer: 80→160 chars, 5→15 words (handles
      Russian phrases, matching answers, multi-word subject answers)

[x] Text-only path: add Llama verification when Kimi confidence < 0.85
    - agree → return; verifier wins on confidence gap; else keep Kimi

[x] System prompts: add Russian school context (ЕГЭ, ОГЭ, ВПР, МЦКО, МЭШ)
    - explicit subject list, matching/option answer format hints
    - token limits increased: solver 1100→1500, verifier 700→1000,
      text-only 700→1200, parser 1200→1400

[ ] Keep JSON failure handling strict:
    - malformed Kimi JSON must not crash worker
    - malformed Qwen/Llama JSON should degrade
    - local salvage must not override stronger structured evidence


GUI / X11 TODO
--------------

[ ] Manual target-machine startup test with `bash run.sh`.
[ ] Ctrl+Space toggles small window.
[ ] Ctrl+Shift+Space shows window.
[ ] Ctrl+Alt+Space processes clipboard text.
[ ] Ctrl+Alt+Space processes clipboard image.
[ ] Ctrl+Shift+S inserts screenshot without unwanted first-window popup.
[ ] Close via `×` hides the window and preserves session.
[ ] Pasting multiple images preserves order.
[ ] Input re-enables after provider errors/timeouts.
[ ] Assistant placeholder `...` clears when answer starts.


TEST COMMANDS
-------------

Local:

  python3 -m py_compile main.py app/*.py ui/*.py tests/*.py
  python3 -m unittest discover -s tests -p 'test_*.py'

Live:

  python3 tests/run_solver_fixture_suite.py user-regression

Known live-suite expected answers:

  101 -> 16
  102 -> 25
  103 -> 23
  104 -> 124
  105 -> 120/17
  106 -> a/sqrt(3) or equivalent
  107 -> 69
  108 -> 80
  109 -> 0.8
  110 -> 6


RELEASE TODO
------------

[ ] Before release, sanitize `.env` in the archive:
    `OPENROUTER_API_KEY=`

[ ] Release archive should match `main` code except for the empty key.

[ ] Do not include local-only logs or generated `/tmp` fixtures.

================================================================================
